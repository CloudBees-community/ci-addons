#!/bin/sh

ADDON=$1
USERHOST=root@forge.cloudbees.com
BASE=/opt/shards/default/data/cloudbees/scm-dav/distributions/ci-addons
                      
echo Deploying addon scripts
ssh ${USERHOST} "mkdir -p ${BASE}/$1/"
CMD="scp $1/*-addon ${USERHOST}:$BASE/$1/"
echo $CMD
$CMD
